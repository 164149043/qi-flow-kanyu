# Product

<!-- impeccable:product-schema 1 -->

## Platform

web（桌面优先的单页应用，鼠标重度交互；暂无移动端/触屏适配）

## Users

**主要用户：风水爱好者。** 出于好奇和研究心态，想直观看见传统风水（八宅方位吉凶、九星飞星流年、五行生克）与室内气流、通风、采光之间的关系的个人探索者。他们打开它是为了"玩着看"——上传自己的户型，摆几个炁口/风口/五行，拖一拖参数，实时观察气场怎么在屋里跑、风水吉凶落在哪。

**使用门槛要低、反馈要即时、要好玩。** 不是给执业风水师做判断依据的专业仪器，也不是给建筑师做工程决策的生产工具——是让爱好者"看得见、摸得着、玩得起"的探索沙盒。

## Product Purpose

把**传统中国风水学**与**现代计算流体力学（CFD，Navier-Stokes 实时模拟）**融合进一个 3D 可视化引擎：用户上传户型图（或用默认户型），在三维场景里放置"源"（炁口/五行/风口/光源），调整环境（风向、时辰），叠加风水层（八宅、九星），实时看见气场浓度、风速涡量、采光如何流动与分布。

**它为什么存在：** 作为作者本人的**技术作品集 demo**——展示把复杂的实时物理模拟（自研流体求解器、Worker 化、MacCormack/涡量）、户型视觉识别、传统术数算法（八宅/九星/五行）整合工程化进一个可分发的单文件 Web 应用的能力。**视觉冲击力与技术炫耀是第一使命**，但底子必须是一个真能用、真能跑的探索工具，而不是空壳特效。

**成功意味着：** 访客打开即"卧槽这牛逼"，愿意摆弄几分钟；懂行的人看得出背后的工程量（自研求解器、Worker、单文件打包、术数算法都自己实现）。

## Positioning

**对标参考是 xunq.chat（独立单文件重制，非官方）。** [inferred——来自项目记忆反复出现的"对齐 xunq 原版"，待用户确认]

真正差异化的机制，是邻居产品难以照搬的"**科学层（CFD 真实流体物理）× 文化层（严谨的八宅九星五行术数）并列融合**"：流体模拟不是贴图的假动画，而是自研的 Navier-Stokes 求解（MacCormack 平流、涡量、Brinkman 障碍）；风水不是装饰标签，而是真正参与炁场泄耗计算的算法层。两者在同一套场里互相作用。

## Operating Context

典型一次使用：
1. 开箱即见一个默认户型的三维场景（浅灰科技底，半透明墙，悬浮罗盘指北）。
2. 顶部切换三种模式：**炁流**（放五行结构 + 炁口，看炁场浓度）、**风场**（放风口 + 调环境风，看速度/涡量热力图）、**采光**（放光源 + 调时辰，看日照）。
3. 也可**上传自己的户型图**，经 PlanEditor 识别/手绘墙、窗，重建三维墙体。
4. 左键在地板上放置或选中"源"，右键拖拽旋转视角，滚轮缩放（选中带方向的源时滚轮改为微调朝向），拖动移动，Delete 删除。
5. 按需叠加**八宅**（大门朝向 → 宅卦 → 方位吉凶扇区）和**九星**（流年 → 九宫飞星 → 吉凶 + 炁场泄耗）风水层。
6. 后台 Web Worker 持续跑流体模拟，画面实时更新。

**环境：** 现代桌面浏览器，鼠标交互；整个引擎打包为单个 HTML 文件，双击即开、随处可分发。

## Capabilities and Constraints

**已实现能力：**
- 实时 CFD 流体模拟（自研 Navier-Stokes 求解器：MacCormack 平流、投影、涡量、Brinkman 障碍处理），Worker 化后台计算。
- 三种可视化模式：炁流（VolumetricFlow 体积粒子云）/ 风场（Canvas2D 速度+涡量热力图，含伪 3D 气动）/ 采光（日照光束）。
- 户型图视觉识别（Sauvola 自适应阈值 + 线条检测）+ PlanEditor 手绘墙/窗/擦除编辑。
- 风水算法层：八宅（方位吉凶）、九星飞星（流年九宫 + 炁场泄耗）、五行（金木水火土生克）。
- 源系统：炁口/风口/光源/五行结构的放置、选中、拖拽、方向与强度调节、删除。
- 环境参数：环境风向/风速（风场）、时辰/强度（采光）、大门朝向（八宅）、流年（九星）。

**技术约束（影响后续所有 UI/视觉工作）：**
- 单文件打包（vite-plugin-singlefile）——一切资源 inline，包括 Worker（`?worker&inline`）。
- 无前端框架——现有 UI 全部 `document.createElement` 手搓 DOM；任何 UI 重构要么延续手搓，要么谨慎评估引入框架的打包体积与单文件兼容成本。
- Three.js 0.144（较旧版本，部分新 API/示例不可用）。
- 桌面鼠标优先，**暂无移动端、触屏、键盘可达性适配**。

**已知缺口 / open decisions（未决定，未编造）：**
- 多楼层：参考图里有"楼层"概念，当前实现是单层。[open]
- 户型/方案的保存、导出、分享：当前无。[open]
- 结果的数值化解读（除了视觉，是否给用户量化指标）：当前仅有热力图/粒子视觉，无数值面板。[open——且与作品集"炫技数据面板"方向相关]

## Brand Commitments

- **产品名：**「炁流」（qì liú）· 副标"3D 风水引擎"。「炁」是风水/道家语境里"气"的专字，命名带有明确的东方术数身份。
- **语言：** 中文界面、中文术语（炁口、风口、八宅、九星、五行、宅卦等），保留专业术数词汇。
- **态度承诺：科学 × 风水并列融合。** 流体力学当作真物理认真实现，风水术数当作严谨的文化算法层认真实现——既不把风水包装成硬科学，也不把它贬为迷信贴纸，两者在同一套场里各自成立、互相作用。文案应维持这种"双重认真"。

## Evidence on Hand

- **自研算法代码即证据：** FluidSolver / fluid.worker.js（流体求解）、Bazhai / Jiuxing / Wuxing（术数）、ImagePreprocess / LineDetector（户型识别）均为自研实现，可在代码库 `src/` 下查验。
- **默认户型 + 实时可演示：** `buildDefaultPlan` 提供开箱即用的演示场景，三种模式、八宅九星叠加均可现场运行展示。
- **无真实客户、案例、证言、媒体报道、第三方数据。** 本项目为个人技术 demo/作品集，**未来任何设计工作不得编造**客户 logo、使用数据、评价或案例。

## Product Principles

1. **技术炫耀是第一使命。** 作为作品集，"看着牛逼、藏着工程量"压倒一切保守稳妥——凡是能让人一眼震惊或细看佩服的，放手做；平庸安全是失败。
2. **科学层与文化层同等认真。** CFD 是真物理、风水是真算法，两者都做到能被懂行的人检验，不靠贴图和标签糊弄。
3. **实时、可玩、低门槛。** 爱好者打开即玩——放源即看见、调参即响应、不读说明书也能摸索出门道。
4. **单文件、自包含、随处可开。** 整个引擎是一个 HTML，是这个 demo 可被轻易分享和验证的核心前提，任何新增依赖都要守住可单文件分发的底线。
5. **探索式交互。** 界面鼓励"摆弄"而非"填表"——直接在三维空间里放置、拖拽、调向，让用户感觉自己在操控一个真实的物理场。
