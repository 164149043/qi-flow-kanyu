---
name: 炁流 · 3D风水引擎
description: 用 CFD 风洞的科学骨架，承载东方堪舆的雅致——一个浅灰、霁青、克制的实时气场可视化引擎。
colors:
  paper: "#ffffff"
  silk-bg: "#eceef2"
  ji-floor: "#d8dde4"
  canvas-floor: "#e1e5eb"
  ink: "#333333"
  ink-soft: "#555555"
  ink-faint: "#999999"
  mist-line: "#c8ccd4"
  ji-qing: "#2b6cb0"
  ji-qing-bright: "#4a80d9"
  ji-qing-pale: "#e8f0fe"
  dai-blue: "#1a56c4"
  dai-ink: "#3a4256"
  shaft-ink: "#2a3242"
  zhe-gold: "#c77800"
  chi-gold: "#d89000"
  wuxing-metal: "#c8d0e0"
  wuxing-wood: "#6abf4b"
  wuxing-water: "#4aa8ff"
  wuxing-fire: "#ff5530"
  wuxing-earth: "#c8a060"
  alert-red: "#c0392b"
  alert-red-edge: "#e0b4b0"
  status-green: "#2a8a3a"
typography:
  body:
    fontFamily: 'system-ui, "Microsoft YaHei", sans-serif'
    fontSize: "13px"
    fontWeight: 400
    lineHeight: 1.5
  label:
    fontFamily: 'system-ui, "Microsoft YaHei", sans-serif'
    fontSize: "12px"
    fontWeight: 400
    lineHeight: 1.4
  caption:
    fontFamily: 'system-ui, "Microsoft YaHei", sans-serif'
    fontSize: "11px"
    fontWeight: 400
    lineHeight: 1.4
rounded:
  sm: "4px"
  md: "6px"
  lg: "8px"
spacing:
  xs: "4px"
  sm: "8px"
  md: "12px"
  lg: "14px"
components:
  button-default:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.ink}"
    typography: "{typography.body}"
    rounded: "{rounded.sm}"
    padding: "6px 14px"
  button-active:
    backgroundColor: "{colors.ji-qing-pale}"
    textColor: "{colors.dai-blue}"
    typography: "{typography.body}"
    rounded: "{rounded.sm}"
    padding: "6px 14px"
  floating-bar:
    backgroundColor: "rgba(255,255,255,0.80)"
    rounded: "{rounded.lg}"
    padding: "8px 12px"
  panel-source:
    backgroundColor: "rgba(255,255,255,0.88)"
    rounded: "{rounded.lg}"
    padding: "12px 14px"
  hint:
    backgroundColor: "rgba(255,255,255,0.80)"
    typography: "{typography.label}"
    rounded: "{rounded.md}"
    padding: "6px 14px"
---

# Design System: 炁流 · 3D风水引擎

## Overview

**Creative North Star: "The Qi Wind-Tunnel"（炁流风洞）**

炁流用**风洞的科学骨架**承载**东方堪舆的雅致气质**。它把一套自研的 Navier–Stokes 流体求解器、户型视觉识别、八宅九星五行术数算法，收进一个浅灰、霁青、克制的单文件界面里。核心姿态是：CFD 是真物理、风水是真算法，但视觉从不喧哗——不霓虹、不炸裂、有耐心。科技站在前台表演，东方退在骨血里定调。

在这套语言里，**浅灰是留白**（素绢般的背景与地板，一层比一层略深，制造无侵略的纵深感），**霁青是测绘的冷静笔触**（墙体的棱线、激活态、罗盘指向都共用这一根线），**赭金是术数的读数**（所有参数数值与选中态用同一种金，像堪舆盘上刻度的填色）。气动热力图是风洞的数据产物，五行五色是东方能量的语义编码，丁达尔光束是采光的体积证据——但它们都被"雅致克制"四个字压着，密度高却不躁。

深度由**真实的 3D 场景**与**悬浮的毛玻璃浮层**共同给出：墙体有高度、粒子云有能量山峦的起伏、雾在远处收边、光束在窗后有体积；而所有信息和控件都活在 `backdrop-filter: blur(8px)` 的悬浮层里，轻而不扁。整套阴影统一用冷蓝灰 `rgba(60,70,90,…)` 而非纯黑——这是本系统的指纹。

**Key Characteristics:**
- 浅灰科技底（素绢/霁灰），留白慷慨，绝不深色霓虹。
- 霁青一根线贯穿：建筑棱线、激活态、罗盘、箭杆共用同一族蓝。
- 赭金读数：参数值与选中环的金，是术数仪表的刻度色。
- 冷蓝灰阴影（非纯黑）+ 毛玻璃悬浮 = 深度的指纹。
- 科学色带（气动谱/炁流谱）做数据，东方五行色做语义，互不混淆。
- 三模式（炁流/风场/采光）各有一套配色逻辑，但共享同一套中性骨架。

**确认的视觉反参考（anti-reference，勿恢复）：** 三维粗线渐变流线（Line2，两次被否决为"难看"）、webgl-wind 拖尾粒子、Physarum 黏菌粒子——这三类都已从代码移除，是明确的视觉拒绝。深色霓虹 HUD 也不是本路。

## Colors

调色板的性格是**冷调、测绘感、低饱和**，靠一根霁青和一抹赭金撑起全部指向性，其余交给留白与数据色带。

### Primary
- **霁青 Ji-Qing**（`#2b6cb0`）：系统的灵魂色。墙体挤出后的 EdgesGeometry 棱线、罗盘指北箭头族、所有"建筑/测绘"语义都落在这根线上。它是浅灰底上唯一被允许大面积出现的冷色骨架。
- **霁青亮 Ji-Qing Bright**（`#4a80d9`）：激活态边框与按钮 hover 边。比霁青更明，用于"当前选中"的强调。
- **黛蓝 Dai-Blue**（`#1a56c4`）：激活态文字。霁青系里最深最饱的一档，保证浅底上的可读对比。

### Secondary
- **赭金 Zhe-Gold**（`#c77800`）：所有滑块数值、参数读数的文字色。术数仪表的"刻度填金"，让每一个数字都像被堪舆盘校准过。读数用金是全系统的统一约定。
- **赤金 Chi-Gold**（`#d89000`）：源（炁口/风口/光源/五行）被选中时的环色，以及八宅大门朝向箭头。比赭金更暖更亮，专属于"被选中的那个东西"。

### Tertiary（五行语义色，不可挪用为 UI 强调）
五行五色是**风水语义的编码**，每一种对应一种对气场的作用机制，只用于五行结构球体与界面里的五行标识，绝不挪作普通按钮或链接色：
- **素银 Metal**（`#c8d0e0`）：金·凝聚（气流减速沉淀）
- **青翠 Wood**（`#6abf4b`）：木·生发（持续生发能量）
- **沧浪 Water**（`#4aa8ff`）：水·流动（加速气场循环）
- **朱砂 Fire**（`#ff5530`）：火·升散（能量向外辐散）
- **缃黄 Earth**（`#c8a060`）：土·承载（减速 + 托能）

### Neutral
- **素绢 Silk-Bg**（`#eceef2`）：主背景与雾色。全系统的留白基底。
- **纸 Paper**（`#ffffff`）：按钮默认底、浮层白系的纯白档。
- **霁灰 Ji-Floor**（`#d8dde4`）：3D 地板，比素绢略深一档，给地面以存在感。
- **画板 Canvas-Floor**（`#e1e5eb`）：热力图在极低值时回落的底色（热力图"空白区"就是它）。
- **墨 Ink**（`#333333`）/ **淡墨 Ink-Soft**（`#555555`）/ **灰墨 Ink-Faint**（`#999999`）：三级正文/次级/占位文字。
- **雾线 Mist-Line**（`#c8ccd4`）：按钮与面板的默认边框。浅灰底上的"勾边但不强调"。
- **黛墨 Dai-Ink**（`#3a4256`）/ **杆墨 Shaft-Ink**（`#2a3242`）：罗盘北向箭头与源方向箭杆的深色，浅底上保证方向指示清晰。

### 功能色
- **警示红 Alert-Red**（`#c0392b`）：删除按钮文字（边 `#e0b4b0`）。仅用于破坏性操作。
- **状态绿 Status-Green**（`#2a8a3a`）：FPS 计数等"运行正常"的低调状态读数。

### 结构件材质色（屏风/门/窗专属，锁定给结构件语义）
- **屏风木 Screen-Wood**（`#c8a060`）：屏风板与按钮色条；支脚深化 `#8a6d4f`。
- **门木 Door-Wood**（`#8b6f47`）：门板；门把手用赤金 `#d89000`（与选中环同源）。
- **窗玻璃 Window-Glass**（`#9fc8e8`）：半透明玻璃面（opacity 0.42）与窗类开关按钮描边；按钮色条用深化版 `#7fb8dc`。框柱沿用墙色 `#e8ecf2`。

### 数据可视化色带（科学语义，非 UI token）
热力图色带是**风洞数据的映射**，按模式切换，独立于 UI 调色板，不在界面上当强调色用：
- **炁流谱 Qi-Spectrum**（炁流/采光模式，`#0a5a24 → #3fae2a → #a5d400 → #ffe600 → #ff8c00 → #ff1e00`）：深绿→翠→黄绿→黄→橙→红，浓度由低到高。gamma `pow(t,0.75)` 提升低值可见。
- **气动谱 Aero-Spectrum**（风场模式，`#0a1450 → #143cb4 → #1ea0dc → #78dce6 → #ffdc50 → #ff321e`）：深蓝→蓝→青→浅青→黄→红，是 CFD 气动标准色，让涡量与速度结构清晰。gamma `pow(t,0.5)`。
- 风场叠加**伪 3D 气动**：迎风面 +0.25 高亮、背风面 ×0.55 空腔暗影、邻格建筑 ×1.15 屋面加速——用色阶落差表达气流绕障碍的物理。
- **丁达尔光束**（采光体积光，additive blending）：内层 `#ffeebb` opacity 0.14、外层 `#ffcc88` opacity 0.07，从落地窗向室内层叠出体积光。

### Named Rules
- **The Cold-Shadow Rule（冷蓝灰阴影法则）.** 所有 UI 阴影必须用 `rgba(60,70,90,…)` 冷蓝灰族，禁止纯黑 `rgba(0,0,0,…)`。冷阴影是这系统区别于通用 Material 的指纹——它让浅灰底不脏、让悬浮层像浮在雾里而非压在纸上。
- **The One-Gold Rule（读数金法则）.** 赭金 `#c77800` 专属于"数值读数"，赤金 `#d89000` 专属于"选中态"。金色不出现在这两个语义之外，尤其不做大面积填充或品牌色。
- **The Wuxing Lock Rule（五行锁定法则）.** 五行五色是语义编码，只绑五行结构。永远不要把沧浪蓝当链接色、把朱砂当错误色、把青翠当成功色——那会破坏风水语义的可读性。

## Typography

**正文字体：** `system-ui, "Microsoft YaHei", sans-serif`（无衬线，系统字体优先，中文回落雅黑）
**显示字体：** 暂无（`[to be resolved]`——未来若要强化东方堪舆气质，可考虑为术数标题引入一款中文衬线/楷体；现状全场无衬线一统）。

**Character:** 克制、工具感、不抢戏。字号层级靠 1px 的精细差与字重 600 的激活态拉开，而非靠大字号冲击。术数术语（炁口、八宅、九星、宅卦）一律全字保留，不缩写、不翻译。

### Hierarchy
- **Body**（400，13px，1.5）：按钮文字、面板标题、源属性标签。界面主语。
- **Label**（400，12px，1.4）：参数行标签、hint 提示、FPS。次级信息与操作引导。
- **Label-Weak**（400，11px，1.4）：源面板底部的操作说明等最弱提示。label 的灰墨降级版。
- **激活态**（600，继承字号）：按钮 `.active`、当前模式。仅靠字重 600 + 霁青淡底区分，不加粗到 700，不变色到非霁青系。

### Named Rules
- **The 1px Discipline Rule（一像素纪律）.** 字号阶梯只有 13 / 12 / 11 三档，差 1px。不要引入 14px、16px 等中间档；层级靠字重与颜色，不靠跳跃的字号。**此律只辖屏上 UI；打印媒介见下。**
- **The Terminology Rule（术数全字法则）.** 炁、炁口、八宅、九星、宅卦、五行等术语必须用中文全字，数值配赭金。界面语言保持堪舆专业感，不做口语化或英文替换。

### Print Hierarchy（打印报告字阶，仅 `@media print` 的 #pdf-report）
纸是另一媒介，不吃 1px 纪律——A4 报告要有文档层级：
- **报告标题**（600，20px，letter-spacing 2px，霁青，底 2px 霁青分隔线）
- **节标题 h2**（600，14px，霁青）
- **报告正文/表格**（400，11px）· **元信息行**（12px）· **脚注**（10px 灰）

## Layout

**空间模型：** 一个全屏 `position:fixed; inset:0` 的 3D 画布作为主舞台，左右两根**贴边停靠的全高栏**（操作台/堪舆数据）从两侧框住舞台，其余 UI（hint）以浮层悬浮——栏是墙，浮层是雾。

- **左侧操作台（`.ctrl-panel`）：** `left:0; top:0; bottom:0`，宽 236px，贴紧浏览器左边框的全高停靠栏（2026-08-15 起不再悬浮，同日由 210px 加宽）。圆角 0、无边缝，仅右缘一根雾线边 + 向内单侧冷蓝灰阴影。内容自上而下：模式分段 → 放置（模式子分组）→ 视图 → 八宅 → 九星 → 源属性（选中源时展开）。标题行右侧挂 FPS 读数。
- **右侧堪舆数据（`.dp-panel`）：** `right:0; top:0; bottom:0`，宽 256px，贴紧右边缘的全高停靠栏，形态同左栏（仅左缘边线 + 向内阴影；2026-08-15 由 226px 加宽，采光曲线/雷达 canvas 随之 224px 填满）。内容五节：气流仪表（环形+玫瑰双并排表盘）→ 采光曲线 → 八宅能量雷达 → 九宫飞星 → 详情。
- **底部居中提示（`.hint`）：** `bottom:12px`，居中于两栏之间的舞台，承载当前操作语境（放置/选中/调向提示），是用户和界面之间的实时对话条。
- **3D 舞台：** 网格 96×80（CELL=0.20m），相机斜俯视（PerspectiveCamera 50°，阻尼 0.08，限位不钻地板）。墙体挤出连续带，粒子云与热力图共享同一片地板纹理。

**间距节奏（spacing）：** `xs:4 / sm:8 / md:12 / lg:14`。浮层内 `gap:8px`，按钮 `padding:6px 14px`，面板 `padding:12px 14px`。节奏偏紧凑——工具型界面，信息密度优先，但靠留白底（素绢）补呼吸。

**响应式：** 桌面优先，鼠标交互**全走左键**（左键=放置/选中/拖源；空白处左键拖=旋转；Shift+左键拖=平移；滚轮=缩放/调向；右键不用）。capture 阶段按命中对象动态让位左键（罗盘指针/源/放置态优先于旋转）。**暂无移动端、触屏适配**——这是一条已知的现状缺口，非设计意图。

**放置域：** 模拟域 96×80 格（19.2×16m）即全部可放区——**建筑内外全域可放**（默认宅为**四合院**：北正房+东西厢房+南倒座+中央庭院+东南巽位门，立于域中央，院外开阔空地 ~95%）；视觉地面为大画布（FW×4.2，雾在 FW*4.5~12 远处起），域外点击给"域外无流体场"提示；源不可落墙/结构件格（hint 指引），门/窗/屏风不受限。

### Named Rules
- **The Floating-Layer Rule（浮层悬浮法则）.** 控件悬浮在 3D 之上，不砌不透明实体面板遮挡舞台。例外：左右两根贴边全高栏（操作台/堪舆数据）是**停靠栏**而非浮层——它们是舞台的画框，允许贴边落地，但仍须 `backdrop-filter: blur(8px)` + 半透明白保持透气，禁止加深成实体侧板。其余浮层（hint 等）照旧悬浮。
- **The Contextual Hint Rule（语境提示法则）.** 底部 hint 是唯一的实时操作引导，必须随当前状态（放置/选中/调向）更新文案。不要用 toast 或弹窗打断流体模拟的沉浸。

## Elevation & Depth

本系统的深度是**真实 3D 与毛玻璃浮层的并重**，不是纯阴影体系。

3D 场景给**物理深度**：墙体有真实高度（默认 1.3m，识别户型时降到 0.85m 不挡视线），炁流粒子云按能量浓度长成"能量山峦"（高至 1.8m），风场粒子低空快速漂移，采光粒子是暖金光毯，雾在 `FW*3 ~ FW*8` 处用素绢色把远景收进留白，丁达尔光束从落地窗向室内叠出体积。

UI 浮层给**信息层的悬浮感**：`backdrop-filter: blur(8px)` 让背后的 3D 模糊透出，半透明白底（80%–88%）+ 冷蓝灰微阴影制造"浮在雾上"而非"压在纸上"的轻盈。阴影故意做得小而冷，不追求 Material 式的大落差投影。

### Shadow Vocabulary
- **浮层阴影 bar-shadow**（`0 2px 10px rgba(60,70,90,0.12)`）：顶部浮层栏、模式栏。最轻的一档。
- **提示阴影 hint-shadow**（`0 2px 8px rgba(60,70,90,0.10)`）：底部 hint。比 bar 更收一点。
- **面板阴影 panel-shadow**（`0 2px 12px rgba(60,70,90,0.15)`）：右侧源属性面板。最重的一档，因为面板信息密度最高、离用户最近。

### Named Rules
- **The Cold-Shadow Rule（重申）.** 阴影只用 `rgba(60,70,90,…)`。Y 偏移 2px、blur 8–12px、alpha 0.10–0.15——克制是关键，深而大的阴影会破坏浅灰雅致。
- **The Blur-Breathes Rule（毛玻璃呼吸法则）.** 浮层必须 `backdrop-filter: blur(8px)`。这个模糊值是"轻盈悬浮·可呼吸"手感的来源——不要加到 16px+ 变成磨砂墙，也不要去掉变回硬纸片。

## Shapes

**形式语言：** 克制的小圆角 + 工程感的挤出几何。

- **圆角（rounded）：** `sm:4px`（按钮）/ `md:6px`（hint）/ `lg:8px`（浮层栏、面板）。三档，全部偏小——拒绝大圆角的"软萌"，保留测绘工具的精密感。
- **墙体：** `ExtrudeGeometry` 挤出的连续带状墙（非体素方块拼接），材质浅灰白 `#e8ecf2`、roughness 0.82、metalness 0.05，配 `EdgesGeometry` 的霁青棱线（opacity 0.55）。轮廓干净，转角竖直棱清晰。
- **源可视化：** 球体（emissive 自发光）+ 方向箭杆（杆墨 `#2a3242`）+ 力场范围圈（淡 alpha disc + 描边环）+ 赤金选中环。一套统一的 `_vis` 机制，四种源（炁口/风口/光源/五行）共享形态语言，只换颜色与是否有方向。
- **罗盘：** 悬浮在建筑上方的可交互堪舆罗盘仪器（`CompassInstrument`）：霁青底盘 + 三层同心环（24山外环 / 八宅中环 / 九星内环，CanvasTexture 贴 CircleGeometry，canvas 上=北 ↔ 世界 -z）+ 赤金门向指针 + 沧浪风向指针（杆+锥，可拖）+ 中心模式徽标。warm-up 期居中放大渐显撑场，流体起后原地缩淡为方位锚点。

### Named Rules
- **The Small-Radius Rule（小圆角法则）.** 圆角不超过 8px。测绘/工程气质要求棱角清晰；需要"柔和"时用颜色和留白去柔，而非用大圆角。

## Components

每个组件以一句性格开头，再给形态、配色、状态。完整 CSS（含 blur/shadow/border）见 `.impeccable/design.json`。

### Buttons
冷静、克制、可呼吸的工具按键；激活时才被霁青点亮。
- **形状：** 小圆角（4px），默认白底墨字雾线边。
- **默认：** `paper` 底 + `ink` 字 + `mist-line` 边，`padding:6px 14px`。
- **激活（`.active`）：** `ji-qing-pale` 底 + `dai-blue` 字 + `ji-qing-bright` 边，字重 600。模式按钮、开关按钮的"当前态"。
- **Hover：** 边色由 `mist-line` 过渡到 `ji-qing-bright`，轻反馈，不位移、不变底色。
- **语义变体：** 工具按钮带左侧 4px 色条（风口 `#66ccff`、光源 `#ffee66`、炁口 `#ffaa44`、五行各色），用色条而非整块填色标识类别——克制。

### Floating Bar（浮层栏）
贴顶居中的悬浮容器，是所有参数与模式的载体。
- **背景：** `rgba(255,255,255,0.80)` + `blur(8px)`，圆角 8px，冷蓝灰阴影 `bar-shadow`。
- **内部：** `gap:8px` 横排 label+控件；多行参数按 top 偏移垂直堆叠，按模式显隐。
- **行为：** 永远悬浮，永远半透——它是贴在 3D 雾气上的一片玻璃，不是实体面板。

### Source Panel（源属性面板）
选中源后从右侧浮出的调参面板。
- **背景：** `rgba(255,255,255,0.88)` + `blur(8px)`，圆角 8px，`mist-line` 边，阴影 `panel-shadow`（最重档）。
- **内容：** 源类型标题（带语义色）→ 方向滑块（炁口/风口有，光源/五行无）→ 强度/炁量/风速/亮度滑块 → 删除按钮（`alert-red` 字）→ 弱提示操作说明。
- **行为：** 选中即现、取消即隐；方向滑块与滚轮调向双向同步。

### Hint（底部提示）
用户与界面的实时对话条。
- **背景：** `rgba(255,255,255,0.80)` + 圆角 6px + `hint-shadow`。
- **内容：** 12px，随状态切换文案（放置/选中/调向）。带 🔥/🌀 等轻量 emoji 前缀做语义锚点，但不滥用。

### Slider Row（参数行）
`label + range + 赭金数值` 的统一三件套。
- **结构：** label（12px 淡墨）+ `<input type="range">` + 数值（12px 赭金 `zhe-gold`，固定 min-width 防跳动）。
- **语义：** 所有可调参数（环境风向/风速、时辰/强度、大门朝向、流年、源方向/强度）共用这一种行——视觉一致性即是可信感。

### 3D Source Visual（三维源可视化）— 签名组件
炁流最 distinctive 的自定义组件：悬浮在地板上的能量源标记。
- **球体：** SphereGeometry，emissive 自发光脉动（`sin(now*0.005)*0.2` 呼吸），颜色绑源类型。
- **方向箭杆：** 杆墨圆柱 + 锥头，bearing 0=北=-z，滚轮/滑块 15° 步进旋转。
- **力场圈：** 淡 alpha 圆盘 + 描边环，半径=注入/发射半径（炁口 5 格、风口 18 格），让"影响范围"可见。
- **选中环：** 赤金 `chi-gold` RingGeometry，选中时显现。
- **统一机制：** 四种源共享 `_vis` group，可选中/拖拽/调向/删除。

### Compass（罗盘）— 签名组件（默认隐藏）
**默认 `group.visible=false` 不挡视线**（用户要求）；视图组「罗盘」开关按需显示。隐藏时 `intersect` 短路返回 null（raycast 不看 visible，防隐形劫持点击）。门向/风向指针、盘面滚轮调参等交互仅在显示时可用；门向另有宅卦按钮常驻入口。
建筑上方悬浮的可交互堪舆罗盘仪器，是方位操作的唯一入口（吃掉了原 bazhaiRow/yearRow/windRow 三条分散参数 bar）。
- **结构：** 霁青半透明底盘（兼 raycast 命中面）+ 三层同心环——24 山外环（固定绝对方位基准，canvas 上=北 ↔ 世界 -z）/ 八宅中环（吉凶扇区 + 星名，随门向重画）/ 九星内环（飞星 8 方位 + 中宫，随流年重画）+ 赤金门向指针（可拖，八宅开时显）+ 沧浪风向指针（可拖，风场模式显）+ 中心模式徽标（赭金/沧浪/暖金随模式）。
- **交互：** 拖指针算盘面 bearing 改 doorFacing/curWindDir；盘面滚轮按上下文调参（九星开=流年±1，风场模式=风速±0.5），实时读数进底部 hint。零 DOM 参数 bar——方位仪器不走浮层。
- **状态机：** warm-up 期 hero 态（scale 1.0 / opacity 1.0 / 全环显当图腾 / 徽标自转脉动 / 悬浮呼吸）撑场；流体首帧 +1.5s 后转 anchor 态（scale 0.5 / opacity 0.7 / 停转 / 按开关显隐），原地缩淡为常驻方位锚点。
- **作用：** 把"门向/风向/流年/风速"四个原本散在浮层滑块里的参数，收摄进一个看得见、摸得着的堪舆仪器——方位操作回归方位仪器本业。

### Destructive Safety（破坏性操作安全）— harden 模式
删除不判死刑、重置不装神秘——一切可逆或可反悔。
- **软删除：** 删除源（按钮或 Shift+Delete）只移出场景与 worker，5 秒内底部 hint 显示"已删除【xx】· 撤销（5 秒内）"链接可原位恢复；连续删除先落地上一笔。期满自动 dispose 并复位 hint。
- **原地二次确认（Reset）：** 重置按钮首击进入 armed 态（文案变"确认重置?"、警示描边），3 秒内再击才执行，超时自动复原——不打断沉浸的确认，不用模态弹窗。
- **防误触键盘：** 裸 Delete/Backspace 不删除（滑块聚焦时按退格曾会误删源），必须 Shift+Delete；Esc 仍是取消选中。
- **配色：** 删除按钮白底 `alert-red` 字 + `#e0b4b0` 边；撤销链接用 `dai-blue` 加粗下划线，是 hint 里唯一可点的字。

### Collapsible Legend（可折叠图例）— clarify 模式
知识不挡路：图例默认收起，想知道的人自己展开。
- **结构：** `<details class="ctrl-legend">` 挂在八宅/九星分组尾部，summary 前缀 ▸/▾ 三角指示，11px 淡墨。
- **内容：** `.lg-chip` 行内条目 = 7px 圆点（与地板扇区/九星同色，取自 BAZHAI_STARS/JIUXING_STARS 的 col）+ 星名（九星带五行后缀如"一白·水"），悬停 title 显示星义（宜忌+化解，出自《八宅明镜》《紫白诀》）。
- **行为：** 默认收起；状态各自独立记忆，不随开关层联动重置。

### Fixtures（结构件三件套：屏风/门/窗）— 阻挡语义组件
家具级结构件，改变气流/炁/光的阻挡拓扑，是「玩法层」的一部分。
- **数据：** `{i, j, bearing, len, open}`，从落点沿 bearing (sin,−cos) 延伸 len 格（`lineCells` 半格步进+对角补格，4-连通防斜缝漏风）。
- **mask 语义：** 屏风=solid 恒挡；门 关=solid 开=无；窗 凿墙后 glass 恒 1（**阳光入口不随开关丢**），关再盖 solid。`baseSolid/baseGlass` 户型快照 + `restampMasks()` 投影重放（loop 限频 setMask），上传户型不丢结构件。
- **墙视觉：** 门/窗格线处墙恒开洞（`rebuildWalls` 从 base 减洞，拖动结束才重建防卡）；开关只动门板/窗扇。
- **视觉：** 屏风=木色薄板(#c8a060)+两端支脚，高 1.5m；门=棕门板(#8b6f47)+赭金门把，高固定 1.95m，开门=绕门轴 lerp 旋转 90°；窗=白框+半透明浅蓝双扇(#9fc8e8, y 0.85–1.85)，开窗=双扇推拉滑向一端。REDUCED 直接吸附。
- **UI：** 放置节常显组（不随模式隐藏）四按钮配 authored SVG；属性面板=方向+长度滑块（2–20 格，双单位读数"格/米"）+门窗「开/关」按钮；滚轮调向、拖动、软删撤销全走既有源机制。

### Onboarding Card（首次引导卡）— onboard 模式
三句话把怕按错的人领进门，然后永远消失。
- **结构：** `.ob-card` 底部居中浮层（bottom:56px，宽 340px），毛玻璃白底 + 雾线边 + panel-shadow；三步圆点进度（当前步前全亮 `dai-blue`）+ 标题 + 正文 + 左"跳过"右"下一步/开始"。
- **文案三步：** ①这是什么（堪舆风洞）→ ②怎么布源（放/拖/调向/Shift+Delete 删除可撤销）→ ③怎么断吉凶（罗盘指针+八宅九星+右栏仪表）。
- **行为：** `localStorage['qiliu-onboarded']` 记住看过；URL 带 `?about` 可重看（常驻帮助入口的轻量替身）。跳过与走完等效。

### Report Export（报告导出）— 分享模式
一个按钮出一纸报告：对方什么都不用装，打开 PDF 就是完整的堪舆分析。
- **位置：** 左栏「报告」节（视图节之下），单按钮「导出 PDF」，配文档形 authored SVG。
- **实现：** `window.print()` + 打印视图——零依赖、零库，符合单文件哲学；用户在打印对话框选"另存为 PDF"。
- **报告内容：** 标题行（日期 · 宅卦坐向 · 门朝 · 流年 N X入中）→ 3D 场景截图（render 后同 tick `toDataURL`，renderer 未开 preserveDrawingBuffer）→ 九宫飞星 3×3 表（方位+星名·五行+吉凶）→ 八宅方位吉凶表（8 方位：星/吉凶/释义）→ 布置清单（四类源的格位+参数）→ 出处脚注（《八宅明镜》《紫白诀》）。
- **打印态：** `@media print` 里 `body>*:not(#pdf-report)` 全隐藏（app/操作台/数据面板/hint 一锅端），报告 720px 居中、`page-break-inside:avoid` 防表格拦腰断。屏上 `#pdf-report{display:none}`，`afterprint` 自动移除 DOM；重复点击先删旧报告防叠加。

## Do's and Don'ts

### Do:
- **Do** 让浅灰（素绢/霁灰）做主角，霁青和赭金只做指向性点缀——留白是这套雅致的根。
- **Do** 所有 UI 阴影用冷蓝灰 `rgba(60,70,90,…)`，Y=2px / blur 8–12px / alpha 0.10–0.15。
- **Do** 所有浮层加 `backdrop-filter: blur(8px)`，保持"浮在雾上"的呼吸感。
- **Do** 让所有数值读数用赭金 `#c77800`、所有选中态用赤金 `#d89000`——金是术数仪表的统一语言。
- **Do** 保持字号只有 13/12/11 三档，靠字重 600 和颜色拉层级。
- **Do** 让 3D 深度（墙高、粒子能量山峦、雾、丁达尔光束）承担主要的"炫技"，UI 退为克制的浮层。
- **Do** 三模式切换时，热力图色带（炁流谱/气动谱）和粒子云配色各自整套切换，但中性骨架（素绢底/霁青线/赭金额）不变。切换本身要过渡：热图走"淡出→低谷换带→淡回"，粒子云走整云淡入淡出（`uFade` uniform）+ 色带互切时 dip 遮色跳——不许瞬跳（reduced-motion 例外，直接换）。
- **Do** 破坏性操作一律给反悔路：删除走 5 秒软删撤销，清空类走原地二次确认，键盘删除必须带修饰键——阿清怕按错，界面的职责是让她按不错。

### Don't:
- **Don't** 恢复三维粗线渐变流线（Line2）、webgl-wind 拖尾粒子、Physarum 黏菌粒子——已被用户明确否决/移除，是视觉死区。
- **Don't** 用纯黑 `rgba(0,0,0,…)` 阴影——它会脏化浅灰底，破坏冷调指纹。
- **Don't** 把五行色挪作 UI 强调（链接/成功/错误）——它们是锁定给风水语义的编码色。
- **Don't** 走深色霓虹 HUD 路线——本系统的反参考，雅致克制要求浅灰留白。
- **Don't** 用大圆角（>8px）或大落差投影——那会毁掉测绘工具的精密感。
- **Don't** 用不透明实体面板框死 3D 舞台——浮层必须透气、悬浮。
- **Don't** 把风洞科学色带（气动谱/炁流谱）当作品牌色或 UI 强调色——它们是数据映射，不是身份色。
